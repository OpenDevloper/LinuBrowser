echo welcome to LinuBrowser Setup this program will install the dependencies required to run LinuBrowser
sudo apt install python3-pip
sudo pip install PyQt5
sudo pip install PyQtWebEngine
echo the script has installed the dependencies now you can close the terminal and run LinuBrowser!

